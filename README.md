# github_exercise

This repository was created for the GitHub exercise assignment.

## Contents
- `work.py` – simple script that prints "Work"
- `get_data.py` – script from the course repo used to download the data files
- `.gitignore` – configured to ignore `data.zip` and the contents of `data/`
